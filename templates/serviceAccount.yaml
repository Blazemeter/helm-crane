{{ if .Values.deployment.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "blazemeter-crane.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    role: {{ .Values.deployment.role }}
    clusterrole: {{ .Values.deployment.clusterrole }}
{{ end }}